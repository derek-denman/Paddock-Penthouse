# F1 Provider Notes

MVP phase uses a provider adapter pattern. Live F1 source is optional and controlled by `ENABLE_F1_LIVE`.

## Expectations
- Map provider payloads to normalized event types.
- Preserve raw payload snapshots for audit/replay.
- If provider is degraded/unavailable, fail over to SIM mode when configured.
